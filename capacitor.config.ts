import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.animythro.app',
  appName: 'AniMythRo',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
};

export default config;
